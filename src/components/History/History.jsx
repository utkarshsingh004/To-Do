import './History.css'
import { useState } from 'react'

function History({text}) {

    // const [history, setHistory] = useState(Array(Number).fill(''))

    return(
        <>
         <p>{text}</p>
        </>
    )
}

export default History